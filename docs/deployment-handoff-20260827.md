# 2026-08-27 部署进度与继续操作

> 2026-08-28 已完成正式切换；当前状态见 [正式部署记录](./production-deployment-20260828.md)。以下是 8 月 27 日历史交接，不再作为当前阻塞。

## 当时状态

**新服务器已导入内容并通过核心业务验收，但尚未替换旧容器或正式前端。**

- 服务器 `/opt/1panel/apps/questions-game-v1` 中的新实例目前仍为 `questions-pocketbase:0.40.1-v1-amd64`，回环端口 18090；原 `pocketbase` 和 API 域名入口未改。
- 新库已有 27 道题、4 份叙事、31 个场次，全部已发布。记录 ID、题序、答案、媒体、BGM、线索和演出字段已核对。29 处首尾空白按服务端规则去除；旧叙事 `from` 查询键已转为记录 ID 关联，不承担新路由或显示功能。
- 5 档默认等级仍为 0/100/250/500/1000 经验；题目经验暂按导入配置 50 设置。实际奖品尚未配置，没有虚构发放实物权益。
- 真实服务器验收：首次改密与旧令牌失效、全员下发快照、重复下发幂等、跨玩家拒绝、管理员角色隔离、原始表保护、并发答题只结算一次、已完成默认跳过、显式重发再次加经验。
- 两个 `migration_check_*_20260827` 验收玩家已停用，保留 3 条测试任务及审计记录，不冒充真实玩家数据。临时导入管理员 `content_import_20260827` 已设为 disabled，重新登录返回 403。

## 已验证备份

- 旧服务真实归档：本机 `Downloads/应用安装包/pocketbase.zip`，SHA256 `fcc1d551c05bafaacd4809e265eb1eca38ca8d5182338292c15867c7d133ce93`。归档包含原 0.22.12 二进制、`pb_data` 和 `pb_migrations`。数据库 integrity_check 为 ok；隔离副本用原二进制启动健康检查 200，原题目集合共 27 条。未验证旧管理员密码；外部媒体不在该备份内，Compose/代理配置仍需另行保存。
- 新库快照 `questions_migrated_20260827.zip` 已在新服务器创建并下载到本机 Downloads。SHA256 `ccc30c431f3b2a543a777f817990fd9ac8daa7d70155846a1791a2e6cc12439b`，完整性检查通过；在隔离副本中用 v1.1 镜像启动，27/4/31 内容及两名停用玩家均保留。此份快照生成时临时导入管理员仍为 active；恢复此份快照后必须再停用该账号。
- 最终快照 `questions_migrated_disabled_20260827.zip` 已在新服务器保存并下载到本机 Downloads，SHA256 `fd5259541053b188b064bdef28011915bf48d795e7e2982b08916bfdd5a30d56`。ZIP 校验、数据库完整性与 27/4/31 内容计数通过；启用的业务管理员和玩家均为 0。应优先保留和使用此份快照。日常业务管理员需由超级管理员另行创建。

## 修复与发布包

验收复现了密码边界问题：PocketBase 0.40.1 原生密码字段拒绝 72 位英文密码，原业务规则却放行，最终返回 500。已将业务/两端密码字符上限改为 71，保留 UTF-8 最多 72 字节的限制。创建、重置和改密的边界回归已加入现有测试。答题页 CSS、布局和演出没有改动。

修复镜像为 `questions-pocketbase:0.40.1-v1.1-amd64`，**尚未上传或应用到服务器**。18 项后端测试、镜像空库/认证/重启/恢复检查、真实新库备份在 v1.1 下的恢复及密码边界检查通过；玩家端相关 3 文件 26 项测试通过，两端 TypeScript 和正式构建通过，契约同步通过。

本机发布目录：

`/Users/sitkin/.codex/visualizations/2026/08/27/01a04258-75b3-77d2-b3f4-3c01d1c9f5d8/questions-1panel-release-v1.1-20260827`

- 后端离线包与 SHA256SUMS；与旧发布包同名但位于独立目录，禁止混淆。
- `questions-player-production.tar.gz`：API 指向 `https://api.sitkin.top`，base 为 `/questions/`。
- `questions-admin-production.tar.gz`：同 API，base 为 `/`。现有实际后台站点路径仍需核实，不能随意解压覆盖其他站点。
- `FRONTEND-SHA256SUMS`、`frontend-release.json` 记录前端校验信息。
- `connect-deployment.command`：用户可在 Mac 上运行并私下输入服务器密码，建立仅本机可用的维护连接；不自动改服务器或重置凭据。

## 阻塞与切换条件

用户已经授权创建临时导入账号、迁移和替换容器；**缺的是可用的服务器认证会话，不是替换授权**。现有 SSH 进程只转发 HTTP，未启用可复用控制连接。本机两把现有 SSH 密钥认证失败，SSH agent 没有身份；没有读取私钥内容或索取聊天密码，也没有绕过认证。

继续时需建立可执行命令的服务器登录（例如运行上面的维护连接脚本，或把已登录的 1Panel 终端交给工具）。然后核对前端目录和 OpenResty 实际代理网络、保存配置备份、启用游戏登录限速（新 PB 设置页当前仍为 Disabled）、配置日常业务管理员，再更新 v1.1 镜像及两端前端。

最终端口安排：停用旧 `pocketbase`、取消其自动重启但保留目录；新服务使用唯一映射 `127.0.0.1:8090:8090`，让现有 API 代理继续访问 8090。确认服务器 18090 不再监听，关闭 Mac 的 19090 隧道和维护连接。未完成替换前，不能声称验证端口已全部关闭；不能只关闭 HTTP 隧道就当作已停止远端容器。

本轮收尾已完成：确认并停止仅负责 `19090 → 18090` 的本机 SSH 进程，`lsof` 未见 19090 监听，访问该地址返回连接失败；移除临时导入密码文件并清空工作变量。本轮创建的隔离恢复/测试容器全部已停止，恢复副本已清理，原始备份文件和验收报告保留。服务器 18090 及旧容器仍未操作；此前的本地开发演示未擅自停止。

切换失败且尚无真实新业务写入时恢复旧代理/前端及旧容器。已有真实答题或核销记录时先停写并备份新库，不能用旧库直接覆盖。
